import { Action } from "../../actions";
import type { RuleContributor } from "../index";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const userRules: RuleContributor = ({ can, cannot, user }) => {};
